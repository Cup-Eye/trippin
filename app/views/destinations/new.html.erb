<p><%= link_to "Back", destination_board_destinations_path(@board) %></p>

<h1> Add Destination: </h1>
<%= form_for([@board, @destination])  do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: 'form-control' %>
  </div>

  <%= f.button :submit %>
<% end %>
