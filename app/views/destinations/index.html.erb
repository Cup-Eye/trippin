<div class="destination-container">
  <p><%= link_to "Back", trip_boards_path(@board), class: "button-secondary" %></p>
  <% @destinations.each do |dest| %>
    <div class="destination-card" >
      <div class="" class="justify-content-around">
        <h1><%= dest.name %><b> Number of votes</b></h1>
      </div>
      <p><%= dest.description %></p>
      <div class="destination-buttons">
        <%= link_to "Edit Destination", edit_destination_path(dest) %>
        <%= link_to "Delete Destination", destination_path(dest),
              method: :delete,
              data: { confirm: "Are you sure you want to delete this Destination?" }%>
      </div>
    </div>
  <% end %>
  <%= link_to "Add Destination", new_destination_board_destination_path(@board), class: "trip-btn"  %>
</div>
